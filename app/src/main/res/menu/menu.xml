<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity">

    <item
        android:id="@+id/locked_data"
        android:icon="@drawable/ic_lock_outline_white_24dp"
        android:title="Protect Data"
        android:visible="false"
        app:showAsAction="always" />
    <item
        android:id="@+id/unlocked_data"
        android:icon="@drawable/ic_lock_open_white_24dp"
        android:title="Protect Data"
        android:visible="true"
        app:showAsAction="always" />

    <item
        android:id="@+id/clear"
        android:icon="@drawable/ic_delete_sweep_white_24dp"
        android:title="@string/menu_clear_assets"
        app:showAsAction="always" />

    <item
        android:id="@+id/add_ticker"
        android:icon="@drawable/ic_add_white_24dp"
        android:title="@string/menu_add_assets"
        app:showAsAction="always" />

</menu>
